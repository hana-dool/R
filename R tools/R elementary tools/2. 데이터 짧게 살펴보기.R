
fix(trees) # 데이터 편집기. 손쉽게 편집 가능  

dim(trees) # 데이터의 갯수 확인

str(trees) # 데이터 타입 확인

head(trees) # 상위 5개의 데이ㅌ만 보여준다.

View (trees) #데이터프레임을 엑셀처럼 테이블형식으로 보여준다.

summary(trees) #데이터의 기초통계량을 보여준다.

trees[is.na(trees)] # 데이터에 na 가 있는지 여부
